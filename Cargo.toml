[workspace]
resolver = "2"

members = [
    "graph-git",
    "convenient-bitbake",
    "convenient-git",
    "convenient-kas",
    "convenient-repo",
    "graph-git-cli",
]

[profile.release]
lto = true
#debug = true
strip = "debuginfo"
